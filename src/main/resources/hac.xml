<configuration>
    <contextName>hac</contextName>
    <shutdownHook/>
    <appender name="RFILE" class="shade.hac.ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${user.dir}/hac.log</file>
        <rollingPolicy class="shade.hac.ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- daily rollover -->
            <fileNamePattern>hac.%d{yyyy-MM-dd}.log.zip</fileNamePattern>

            <maxHistory>30</maxHistory>
            <totalSizeCap>500MB</totalSizeCap>

        </rollingPolicy>

        <encoder>
            <pattern>%-4relative [%thread] %-5level %logger{35} - %msg%n</pattern>
        </encoder>
    </appender>
    <appender name="FILE" class="shade.hac.ch.qos.logback.core.FileAppender">
        <file>${user.dir}/hac.log</file>

        <encoder>
            <pattern>%contextName %date %level [%thread] %logger{10} [%file:%line] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="shade.hac.ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%contextName %d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ASYNC" class="shade.hac.ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="RFILE"/>
    </appender>

    <!--<logger name="com.example.hc" level="off"/>-->
    <root level="debug">
        <appender-ref ref="ASYNC"/>
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>